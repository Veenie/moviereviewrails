<h1> Reviews for <%= @movie.title %> </h1>
<h3><% @movie.reviews.each do |r| %>
    <%= link_to r.header, movie_review_path(r.movie_id, r) %> by 
    <%= link_to r.user.name, user_path(r.user_id) %>
    <br>
    <br>
    
<% end %>    

<br>
<br>
<%= link_to "Create new review for this movie", new_movie_review_path(@movie.id) %>
</h3>