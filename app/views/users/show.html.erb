<h1>Hello, <%= @user.name %>!</h1>

<h2>The following is a list of all your reviews! </h2>

<h3><% @user.reviews.each do |r| %>
    Review Title: <%= link_to r.header, movie_review_path(r.movie_id, r) %> 
    <br>
    Movie: <%= link_to r.movie.title, movie_path(r.movie_id) %>
    <br>
    <br>
    <% end %> </h3>