<h1> All Reviews </h1>

<% @reviews.each do |r| %>
    <br>
    <h3> Film: <%= r.movie.title %>
    <br>
    Review Title: <%= link_to r.header, movie_review_path(r.movie_id, r) %>
    </br></h3>
<% end %>